# Bootstrap Admin — planned

This example will demonstrate a familiar responsive administrative interface.

It must demonstrate:

- Bootstrap CSS distributed through the asset pipeline;
- accessible server-rendered tables, forms, validation, and navigation;
- Bootstrap JavaScript excluded unless a feature declares a specific use;
- ordinary postback/form fallback for every mutation.
