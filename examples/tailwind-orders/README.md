# Tailwind Orders — planned

This example will demonstrate a small order workflow styled with Tailwind CSS.

It must demonstrate:

- a reproducible, pinned frontend dependency setup;
- Tailwind content scanning over feature-owned HTML templates;
- final CSS published through the Weft asset manifest;
- no Tailwind runtime JavaScript;
- a normal form fallback and an optional enhanced server action.
