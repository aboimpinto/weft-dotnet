# Pico Portal — planned

This example will demonstrate a lightweight customer portal using Pico CSS or a
comparable CSS-first framework.

It must demonstrate:

- a small static stylesheet and no required JavaScript framework;
- server HTML that remains readable and usable on its own;
- a clear contrast with the Tailwind and Bootstrap integration paths.
