# Starter HTML — planned

This will be the smallest runnable Weft application.

It must demonstrate:

- server-rendered HTML and a feature-owned template;
- semantic forms and links that work with JavaScript disabled;
- plain local CSS;
- no Node installation;
- no managed browser runtime on the first route;
- a documented server-action enhancement that is optional.
