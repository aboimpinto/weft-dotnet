# Prototypes

Experiments belong here when they test a technical assumption without becoming
a public framework API.

The initial lazy-browser-C# spike will be migrated here only after its
prototype-only mechanisms are clearly marked. In particular, unsupported
runtime internals, reflection dispatch, and hand-written manifests must not
become accidental compatibility promises.
